你是一名短剧分镜故事板视觉导演。请根据给定单镜头分镜 JSON 和原文片段，生成一张包含 {panel_grid_size} 个分镜格的宫格图。

【⚠️ 参考图使用规则 - 最高优先级，必须严格遵守】
本次生成会附带角色/场景参考图，所有分镜格都必须严格参照这些参考图绘制，禁止凭空想象人物或场景：
- 角色参考图：每个分镜格中角色的外貌、面部特征、发型、服装、配饰、体型都必须与角色参考图严格一致，不得改动或重新设计。
- 场景参考图：用于参考环境的构图布局、建筑结构、风格与氛围；可按各分镜格的镜头角度重新取景，但场景的视觉特征必须来自参考图，不要直接把参考图整张贴在背景上。
- 参考图上方的文字标签标注了角色/场景名称，请与分镜 JSON 中出现的角色/场景对应；文字标签仅供识别，禁止画入图中。
- 美术风格、线条、色彩、质感、光影都必须与参考图保持一致，禁止出现与参考图风格不符的画面。
- 即使各分镜格表现不同 angle/瞬间/动作分解，人物与场景的视觉特征也必须始终锁定参考图：跨所有分镜格保持同一人物、同一服装、同一场景，不允许某一格偏离参考图另起一套形象。

要求：
1. 整张图为一张完整宫格图，不是单个镜头。
2. 按 {grid_layout} 排列，共 {panel_grid_size} 个分镜格，阅读顺序从左到右、从上到下。
3. 这 {panel_grid_size} 个分镜格围绕同一镜头的主体（角色/场景/动作），表现不同的 angle、瞬间、构图变体或动作分解，**而非 N 个独立故事板镜头**。
4. 全图保持统一画风（以参考图为准）：{style}
5. 整张图片比例为 {aspect_ratio}（每个子格被压缩到对应行列大小）。
6. 不要生成中文或英文文字、水印、编号、对白气泡和界面元素。
7. 分镜格之间需要有清晰边界，但不要让边框压过主体。
8. 角色外貌、服装、场景和道具需要在多个分镜格中保持一致，且必须以参考图为准。
9. 分镜 JSON 的 context.neighbor_panels 包含相邻镜头信息，仅用于保持镜头语言连贯，禁止把相邻镜头内容画进本宫格图。
10. 如果「原文片段」包含台词内容，请根据分镜格的时间顺序，在对应格子中表现角色说话的口型和表情变化：
    - 开始说话：嘴巴微张，表情起始状态
    - 说话中：嘴巴开合变化，表情随情绪递进
    - 说完：嘴巴闭合或保持最终表情
    确保角色口型变化与台词内容的情感和节奏匹配。

镜头分镜 JSON：
{storyboard_text_json_input}

原文片段：
{source_text}
